@model  List<testnorm.Models.Book>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@foreach (var book in Model)
{
    <h1>@book.Name</h1>
    <p>@book.Genre</p>
    <p>@book.Author</p>
    <div>
        @Html.ActionLink("Информация","Details", new { id = book.Id }, null)
    </div>
}

